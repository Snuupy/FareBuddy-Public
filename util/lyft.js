var coordinates = require('./coordinates');



	function lyft.getLyftData(from, to, callback) {
		// /v1/cost
		//check if auth token exists, if not, create one
		//store the token in mongo
		//if token exists and isn't expired, use it
		//if it's expired, create a new one and use it
		//get lyft data
		// config.lyftclient
		// config.lyftsecret
	}
// module.exports = ;